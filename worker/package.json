{
  "name": "@kira/worker",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test:filing": "ts-node --transpile-only src/filing-worker.ts",
    "test:sync": "ts-node --transpile-only src/nightly-sync.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0",
    "bullmq": "^5.4.0",
    "dotenv": "^16.4.5",
    "ioredis": "^5.3.2",
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "prisma": "^5.13.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
